import { VaildationsPipe } from './vaildations-pipe';

describe('VaildationsPipe', () => {
  it('create an instance', () => {
    const pipe = new VaildationsPipe();
    expect(pipe).toBeTruthy();
  });
});
